## DEPRECATED — Removed

This spec has been removed during the Simplify refactoring. The `phase-reminder.ts` hook was deleted along with the Phase management system. The orchestrator agent prompt (`src/agents/prompts/orchestrator.md`) now contains its own workflow guidance directly, and workflow management is handled by OpenCode's superpowers skill chain.
